<<<<<<< HEAD
﻿---
=======
---
>>>>>>> 8a94f4a (skill, agent, hanes)
description: flight_logic의 상태천이 조건과 카운터 로직을 검토한다
allowed-tools: Read Grep
---

`flight_logic/flightlogicapp.py`의 상태천이 로직을 검토하라.

## 검토 항목

### 1. 상태 정의 확인
현재 정의된 상태 enum/상수와 `lib/config.py`의 정의가 일치하는지 확인.

### 2. 천이 조건 표 생성
<<<<<<< HEAD
```
FROM -> TO | 조건 | 카운터 임계값 | 카운터 변수명 | 코드 위치
```

### 3. 조건 누락/비대칭 탐지
- APOGEE 건너뛰기 경로 (ASCENT -> RELEASE) 코드 존재 확인
- 각 카운터가 천이 직후 초기화되는지 확인
- SS 강제 전환 시 카운터 미초기화 부작용 확인

### 4. 시뮬레이션 모드 분기
- SIM ACTIVATE 상태에서 고도 입력 경로 분리 여부
- SIMP 명령으로 입력된 고도가 올바르게 적용되는지

### 5. 타이밍 계산
Barometer 10Hz 기준, 각 카운터 임계값의 실제 소요 시간(초) 계산.
max_alt 갱신 타이밍과 APOGEE 감지 조건 간 레이스 가능성 분석.
=======
각 천이를 다음 표로 정리:
```
FROM → TO | 조건 | 카운터 임계값 | 카운터 변수명 | 코드 위치
```

### 3. 조건 누락/비대칭 탐지
- 역방향 천이(emergency) 처리 여부
- APOGEE 건너뛰기 경로 (`ASCENT → RELEASE`) 코드 존재 확인
- 각 카운터가 천이 직후 초기화되는지 확인

### 4. 시뮬레이션 모드 분기
- `SIM ACTIVATE` 상태에서 고도 입력 경로 분리 여부
- `SS <0~5>` 강제 상태 전환 코드의 부작용(카운터 미초기화 등) 확인

### 5. 의존 데이터 타이밍
- 고도: Barometer 10Hz 기준, 카운터 임계값의 실제 소요 시간(초) 계산
- 최고고도(`max_alt`) 갱신 타이밍과 `APOGEE` 감지 조건 간 레이스 가능성
>>>>>>> 8a94f4a (skill, agent, hanes)

## 출력 형식
이상 없으면 "정상", 문제 발견 시 항목별 심각도(높음/중간/낮음)와 수정 제안 포함.
