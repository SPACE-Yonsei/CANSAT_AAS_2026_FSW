# CANSAT_AAS_2026_Distance
거리측정센서 (TF-Luna I2C)

## 솔레노이드용 거리 필터
FOV가 좁아 순간 오인식이 날 수 있으므로 **중앙값 필터**가 기본 적용됩니다.

- **환경변수** `DISTANCE_FILTER_WINDOW`: 윈도우 크기 (기본 5). 최근 N개 유효값의 중앙값을 출력.
- `0`으로 설정하면 필터 비활성화(원본 값 그대로).
- 범위 밖(200~8000mm 외) 읽기는 윈도우에 넣지 않고, 기존 윈도우의 중앙값을 유지.
